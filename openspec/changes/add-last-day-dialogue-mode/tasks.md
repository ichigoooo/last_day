## 1. 对话协议与提示词

- [x] 1.1 新增 `DialogueSession`、`DialogueTurn` 及相关子模型，为开场对白、续聊结果、结束标记和默认值建立统一解析入口
- [x] 1.2 扩展 `EncounterFrame` 与 `LastDayTurnResult`，使其能够声明当前呈现模式，并在首次进入对话时携带开场对话负载
- [x] 1.3 更新遭遇渲染提示词，使其能够决定 ambient/dialogue，并在进入对话时返回人物信息与开场对白
- [x] 1.4 新增面对面对话续聊提示词契约，覆盖对象发言、玩家回应选项、自定义提示和自然结束结果
- [x] 1.5 为对话相关 JSON 增加字段级默认值、内容安全清洗和 API 失败兜底

## 2. 导演流程与运行时状态

- [x] 2.1 扩展 `WorldState`，增加活动中的面对面对话会话状态，并确保新局重置与阶段切换时正确清理
- [x] 2.2 重构 `LastDayDirector` 的普通行动路径，使其在遭遇结果要求进入对话时创建并保存 `ActiveDialogueSession`
- [x] 2.3 在 `LastDayDirector` 中新增“活动会话续聊”路径，使对话期间输入绕过普通行动解析并推进当前对话
- [x] 2.4 为玩家主动结束对话和系统自然结束对话实现统一的会话关闭与模式恢复逻辑
- [x] 2.5 为每轮面对面对话接入系统定义的小幅时间推进，同时保持金钱、电量、消息等资源边界仍由系统掌控

## 3. Last Day 界面模式切换

- [x] 3.1 调整 `last_day.tscn`，补齐对话模式所需的对象高亮、说话内容区和“结束对话”入口
- [x] 3.2 重构 `LastDayScreen`，让其能够按当前呈现模式分别渲染 ambient 视图和 dialogue 视图
- [x] 3.3 在对话模式中将三颗主选项与底部输入区重映射为“回应”语义，并在退出对话后恢复“行动”语义
- [x] 3.4 在对话模式中禁用“前往场所”“手机”“死神花钱”等入口，并在对话结束后按规则恢复
- [x] 3.5 确保对话结束、切回 ambient 或新遭遇覆盖时，旧人物高亮、旧按钮文案和旧输入提示都会被清干净

## 4. 活动日志与总结链路

- [x] 4.1 扩展 `SessionActivityLog`，增加面对面对话相关的结构化记录类型与必要字段
- [x] 4.2 在进入、继续和结束面对面对话时记录对象发言、玩家回应和结束事件，并保留说话归属
- [x] 4.3 更新 `WorldState.RecentTurnLines` 或等效近期摘要来源，使其能纳入面对面对话内容或摘要
- [x] 4.4 更新故事压缩与终局上下文组装逻辑，确保面对面对话历史会进入总结、悼词或其他收束生成输入

## 5. 兼容、回退与验证

- [x] 5.1 为无 API、非法 JSON、缺失字段或异常结束的情况定义最小可玩的对话回退或安全降级到 ambient
- [x] 5.2 确认对话模式与现有 `NarrativeTurn` 兼容层共存期间不会破坏当前 `LastDay` 主流程
- [x] 5.3 补充最小测试场景或手动验证入口，覆盖“进入对话”“续聊”“结束对话”三类主路径
- [x] 5.4 手动验证对话期间入口禁用、退出后恢复、以及对话日志进入总结链路三类关键验收点
