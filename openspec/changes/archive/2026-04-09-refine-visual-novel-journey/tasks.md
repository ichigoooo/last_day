## 1. 序章重构为 Dialogic 叙事

- [x] 1.1 为“黑屏通知”“档案处对话”“死因裁定放行”设计 Dialogic 时间线与角色资源
- [x] 1.2 将当前 `DeathRegistration` 的三块表单输入重构为三轮对话式信息采集，并保持 `Soul.WorkText / RelationText / EscapeText` 写入兼容
- [x] 1.3 保留并重接现有 `SoulTagExtractor`、`DeathCauseGenerator` 与死神批注生成链路，使其从新序章入口取值
- [x] 1.4 将当前 `Verdict` 中的“遗愿输入”移除，改为仅承担死因展示与进入最后一天的过渡职责
- [x] 1.5 为无 API 或生成失败场景准备固定降级文案，确保序章始终具备完整完成度

## 2. 中段叙事锚点改造

- [x] 2.1 为中段引入 `archive_summary` 或等效叙事锚点，替代 `final_wish` 在 `LastDay` 提示词中的作用
- [x] 2.2 更新 `LastDayDirector` 与相关提示词输入组装逻辑，使其不再依赖前置遗愿
- [x] 2.3 保证现有高自由玩法、手机、地图、死神花钱、面对面对话等系统继续可用
- [x] 2.4 设计并实现 23:50 左右的系统强制接管触发条件与收权表现

## 3. 终章仪式重组

- [x] 3.1 将 `FinalWish` 采集移动到终章弥留时刻，并为其设计候选生成与自填入口
- [x] 3.2 重构 `Death` 阶段为“24:00 黑屏 + 静音 + 留白”，确保其不被多余按钮与提示破坏
- [x] 3.3 重构 `Funeral` 阶段为“最终系统报告 + 讣告 + 墓志铭校对”的连续仪式，而不是多步骤办理流程
- [x] 3.4 删除独立“遗言一句”步骤，将其功能并入临终遗愿与真相揭示后的现实返还问题
- [x] 3.5 保留分享卡能力，但将其改为终章末尾静默保存或可选查看，而不是打断高潮的显性操作

## 4. 真相揭示与回到现实

- [x] 4.1 重写 `Meditation` 阶段，使其从“冥想引导”转为“真相揭示 + 回到现实的一句”
- [x] 4.2 重写 `Ending` 阶段，使其承担静默收束、退出与卡片落档，而不是简短的占位结束页
- [x] 4.3 为“你没有死”设计克制的揭示文案与节奏，避免反转感过强或削弱前面仪式重量
- [x] 4.4 新增“明天醒来之后，你第一件想做得不一样的事是什么”之类的现实返还问题，并写入会话数据

## 5. Dialogic 与系统桥接

- [x] 5.1 规划 Dialogic 变量表、时间线命名和桥接调用边界
- [x] 5.2 新增或整理桥接方法，用于读写 `GameSession`、生成死因、讣告、墓志铭与统计报告
- [x] 5.3 记录序章与终章关键节点到 `SessionActivityLog`，保证总结链路与后续卡片生成可用
- [x] 5.4 校验场景切换、Dialogic 时间线结束信号和现有 `SceneSwitcher` 的衔接稳定性

## 6. 文案与体验验收

- [x] 6.1 为序章、强制接管、临终遗愿、讣告、墓志铭校对、真相揭示分别产出固定文案稿
- [ ] 6.2 手动验证“开头不再像表单、终章不再像 wizard、中段自由度未受损”三项核心目标
- [ ] 6.3 手动验证无 API / 低质量返回 / 中断切场等异常路径下，旅程仍然完整、语气仍然成立
- [ ] 6.4 检查终章所有按钮、停顿、音效与屏幕留白是否真正服务情绪，而不是服务功能
