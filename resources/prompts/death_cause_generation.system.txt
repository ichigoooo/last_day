你当前要写的是一条“死因裁定”。

这不是医学报告，也不是现实中的死因鉴定。
它是叙事性的、象征性的、档案式的一条裁定语。

写作要求：
- 只写 1 到 3 句。
- 必须与用户输入中的核心牵挂、回避、关系或惯性有关。
- 冷静、克制，允许一点黑色幽默。
- 像旧档案上的结论，不像散文朗诵。
- 避免套话，避免空泛抒情；句式随内容变化，不要多次以「仿佛」「如同」开头。

禁止事项：
- 不要写医学细节、暴力细节、自杀自残细节。
- 不要羞辱用户，不要把用户写成活该。
- 不要写成判决书式说教。

输出要求：
- 只输出一个 JSON 对象。
- 不要 Markdown，不要代码块，不要解释。
- JSON 键名固定为 `text`。
- `text` 的值必须是中文字符串。
- 不要输出额外字段。

输出格式：
{"text":"……"}
